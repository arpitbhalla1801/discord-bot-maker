# Database Configuration
# Get your Neon Postgres connection string from: https://neon.tech
# Use the pooled connection string (with -pooler in the hostname)
DATABASE_URL=postgresql://username:password@ep-xxxxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# Better Auth Configuration
BETTER_AUTH_SECRET=your-secret-key-here-generate-a-random-string
BETTER_AUTH_URL=http://localhost:3000

# For production, use your production URL:
# BETTER_AUTH_URL=https://yourdomain.com
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# Discord OAuth Application (for user login)
# Get these from: https://discord.com/developers/applications
# Add this redirect URI in Discord OAuth2 settings: http://localhost:3000/api/auth/callback/discord
# Required scopes: identify, email, guilds (configured automatically)
# The 'guilds' scope allows fetching user's Discord servers for streamlined deployment
DISCORD_CLIENT_ID=your-discord-client-id
DISCORD_CLIENT_SECRET=your-discord-client-secret

# Discord Bot (Single Shared Bot for all projects)
# Create a bot in your Discord Application
# Get the bot token from Bot tab → Reset Token
# Get the Application ID from General Information → Application ID
# IMPORTANT: Keep these secret! Never commit to git
DISCORD_BOT_TOKEN=your_bot_token_here
DISCORD_BOT_ID=your_application_id_here
DISCORD_BOT_PUBLIC_KEY=your_public_key_here

# Public bot ID (safe to expose in frontend)
NEXT_PUBLIC_DISCORD_BOT_ID=your_application_id_here
